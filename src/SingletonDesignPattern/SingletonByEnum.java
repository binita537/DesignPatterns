package SingletonDesignPattern;

public enum SingletonByEnum {


    //Enum -no one can not create instance for enum and override theis Enum
    //Enum can handle serilized and deserrilized

    INSTANCE;

    public void getConfigration()
    {

    }
}
